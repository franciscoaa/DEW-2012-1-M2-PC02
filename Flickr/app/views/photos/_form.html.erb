<%= form_for(@photo, :url => "user") do |f| %>


  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :machine_tags %><br />
    <%= f.text_field :machine_tags %>
  </div>
  <div class="field">
    <%= f.label :per_page %><br />
    <%= f.text_field :per_page %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
